@model IEnumerable<SysComedorCADE.Models.TipoPago>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DetTipoPago)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DetTipoPago)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodTipoPago }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodTipoPago }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodTipoPago })
        </td>
    </tr>
}

</table>
