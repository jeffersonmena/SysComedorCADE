@model IEnumerable<SysComedorCADE.Models.TipoPersona>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Detalle)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Detalle)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodTipoPer }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodTipoPer }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodTipoPer })
        </td>
    </tr>
}

</table>
